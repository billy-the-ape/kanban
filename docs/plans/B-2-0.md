# B-2-0 — Trace model metadata and the effective request pipeline

Execution card for **B-2.1** of [B-2.md](./B-2.md). Read B-2.md first for the milestone purpose, requirements, invariants, and task-packaging rules; this card is one scoped execution unit.

| Field | Value |
| --- | --- |
| Identity | B-2 / B-2.1 / card B-2-0 — metadata trace + session-start diagnostics |
| Starting point | fork `billy-the-ape/kanban`, branch `feat/b-1-thru-5`, base SHA `7f272590f185c284e49c9fe2daa2f9a44aae88cf` (record actual HEAD at start; SDK `@clinebot/core` 0.0.38) |
| Prerequisites | B-1 regression fixtures available (`test/utilities/fake-openai-provider.ts`, `test/utilities/cline-session-service-harness.ts`) |
| Objective | One behavior change: every Cline session start logs the effective context metadata (provider, model, base URL host, context limit + its source, SDK version), and a written trace records where model capacity metadata is produced, consumed, and lost. |
| Context | B-2.md §B-2.1, §Evidence (provider contract carries no capacity), §Architecture (context policy adapter). Re-read current code before edits; note drift. |

## Known metadata path (verify against current code)

1. UI settings → SDK provider settings (`src/cline-sdk/cline-provider-service.ts`: `saveProviderSettings`, `getSelectedProviderSettings`).
2. `resolveLaunchConfig` (`cline-provider-service.ts:~782`) → `ResolvedClineLaunchConfig` (providerId/modelId/apiKey/baseUrl/reasoningEffort only — no capacity).
3. `src/trpc/runtime-api.ts:168` `startTaskSession` → `clineTaskSessionService.startTaskSession` (`src/cline-sdk/cline-task-session-service.ts:~59` `StartClineTaskSessionRequest` — no context fields).
4. `src/cline-sdk/cline-session-runtime.ts:~207` `sessionHost.start({ config, localRuntime })` — passes providerId/modelId/apiKey/baseUrl/reasoningEffort/systemPrompt only; **no `compaction`, `providerConfig`, or `knownModels`**.
5. SDK model metadata sources that exist but are discarded: SDK catalog `ModelInfo.contextWindow`/`maxTokens` (`@clinebot/llms` catalog types, re-exported from `@clinebot/shared`), LiteLLM `/model/info` (`LITELLM_MODEL_LIST_PATHNAMES`, `cline-provider-service.ts:~62` — passthrough schema keeps extra fields, mapper reads only `id`/`model_name`), and `SdkProviderSettings.contextWindow?` (SDK `provider-settings.d.ts:~151` — already in the SDK schema).
6. Discard points: `toSdkProviderModel`/`toSdkProviderModelFromCatalog` (`src/cline-sdk/sdk-provider-boundary.ts:~321/~331`), `toRuntimeProviderModel` (`cline-provider-service.ts:~229`), `runtimeClineProviderModelSchema` (`src/core/api-contract.ts:~697`).
7. SDK compaction defaults that currently govern unconfigured: `DEFAULT_CONTEXT_WINDOW_TOKENS = 200000`, `DEFAULT_THRESHOLD_RATIO = 0.95`, `DEFAULT_RESERVE_TOKENS = 16384` (`@clinebot/core` `dist/extensions/context/compaction-shared.d.ts`).

## Implementation

1. Re-read the path above; record drift from this card.
2. Create `docs/plans/B-2-0-trace.md`: the hop-by-hop trace with file:line evidence, marking each capacity discard point and which SDK defaults currently govern.
3. In `src/cline-sdk/cline-session-runtime.ts`, add one diagnostic log at session start via `createKanbanClineLogger` recording: `providerId`, `modelId`, `baseUrl` host (never the key or full URL path), resolved context limit + source (placeholder `"unconfigured-sdk-default"` until B-2-2 lands), and `@clinebot/core` package version.
4. Extend one existing harness test (e.g. `test/runtime/cline-sdk/` suite using `cline-session-service-harness.ts`) to assert the log line is emitted at start.

## Verification

- `npm test` (full unit/integration), `npm run typecheck` (or the package.json equivalent).
- Manual: start one task against the local provider; confirm the log line and that no credential/path leaks.

## Acceptance

- Trace doc exists, is accurate (reviewable against code), and names every discard point.
- Log line emitted on every session start and restart; zero behavior change to requests or compaction (all pre-existing tests pass).

## Deployment

None.

## Handoff

Changed files, trace doc path, SDK version, unresolved SDK questions (e.g. whether `providerConfig.contextWindow` is honored at runtime by 0.0.38 — types alone do not prove it).

## Stop conditions

- Metadata path diverges materially from "Known metadata path" (drift → update B-2.md evidence before proceeding).
- B-1 fixtures unavailable or the local inference endpoint is down.
