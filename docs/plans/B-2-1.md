# B-2-1 — Carry context capacity through the runtime contract

Execution card for **B-2.8** of [B-2.md](./B-2.md). Read B-2.md first for purpose, requirements (especially #8: verify the real server limit; do not assume 262k), invariants, and packaging rules.

| Field | Value |
| --- | --- |
| Identity | B-2 / B-2.8 / card B-2-1 — `contextWindow` in the provider-model contract |
| Starting point | fork `billy-the-ape/kanban`, base SHA `7f272590f185c284e49c9fe2daa2f9a44aae88cf` (record actual HEAD; SDK 0.0.38) |
| Prerequisites | B-2-0 (trace confirms the discard points); B-1 |
| Objective | `RuntimeClineProviderModel` carries an optional `contextWindow` capacity field, populated from the SDK model catalog and the LiteLLM `/model/info` route where each supplies one; **absence means unknown, never unlimited**. |

## Implementation

1. `src/core/api-contract.ts:~697` — extend `runtimeClineProviderModelSchema` with `contextWindow: z.number().int().positive().nullable().optional()` (nullable encodes "explicitly unknown"; final field name follows the SDK schema — confirm against `@clinebot/shared` `ModelInfo` before editing).
2. `src/cline-sdk/sdk-provider-boundary.ts` — add `contextWindow?: number | null` to `SdkProviderModel` (~line 73); map it in `toSdkProviderModelFromCatalog` (~331) from the catalog model's `contextWindow`, and in `toSdkProviderModel` (~321) if `SdkLocalProviderModel` carries one (check the SDK type; if not, leave undefined — do not invent values).
3. `src/cline-sdk/cline-provider-service.ts` —
   - `toRuntimeProviderModel` (~229): pass `contextWindow` through.
   - `LITELLM_MODELS_RESPONSE_SCHEMA` (~62): extend the item object to capture `max_input_tokens` (numeric, optional) alongside `id`/`model_name`; in the `/model/info` mapping use `max_input_tokens` → `contextWindow`. The passthrough schema already retains the field; the mappers must read it.
   - The fallback single-model entry (configured model id, ~line 900) stays without `contextWindow`.
4. Reconcile fixtures: `web-ui/src/hooks/use-runtime-settings-cline-controller.test.tsx` (models with `contextWindow: null` at ~513/~570/~624) must now expect the field to **survive** validation instead of being stripped; update any other test fixtures producing provider-model objects (search `test/` and `web-ui/src` for `supportsReasoningEffort` fixtures).
5. No UI display in this card (B-10 owns display; B-2-2 consumes the field).

## Scope / non-goals

- Contract + mapping + tests only. No budget logic, no session-start wiring, no SDK dependency change.

## Verification

- Unit tests in `test/runtime/cline-sdk/`: schema round-trip (number, `null`, absent all valid); LiteLLM `/model/info` response with `max_input_tokens` maps to `contextWindow`; `/models`-only response leaves it absent; SDK catalog model with `contextWindow` maps through `listSdkProviderModels`.
- `npm test`, typecheck.

## Acceptance

- For a local model whose `/model/info` reports `max_input_tokens`, the models endpoint exposes it; models without the field report absent/`null`; no consumer may interpret absence as unlimited (asserted by B-2-2 resolver tests).
- Web UI settings tests pass with the reconciled fixtures.

## Deployment

None — additive optional field; older responses remain valid.

## Handoff

Changed files, final field name + nullability decision, which SDK sources actually supplied values on the deployed llama-swap/LiteLLM endpoint (evidence), fixture list updated.

## Stop conditions

- SDK 0.0.38 model catalog exposes no `contextWindow` at all (drift from trace) → proceed with LiteLLM-only population, record the gap for the SDK.
- Field-name conflict with an SDK release in flight → follow the released SDK name, note in patch ledger.
