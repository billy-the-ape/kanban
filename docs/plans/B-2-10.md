# B-2-10 — B-2 acceptance validation and handoff record

Execution card closing **B-2** (validates B-2.1–B-2.8; see [B-2.md](./B-2.md) "Acceptance and tests" and "Handoff"). Cards B-2-0 … B-2-9 must be complete or explicitly parked with evidence before this card starts.

| Field | Value |
| --- | --- |
| Identity | B-2 / closing / card B-2-10 — acceptance matrix + handoff |
| Starting point | fork `billy-the-ape/kanban`, base SHA `7f272590f185c284e49c9fe2daa2f9a44aae88cf` (record actual HEAD after B-2-0…B-2-9 merge) |
| Prerequisites | B-2-0 … B-2-9 receipts (changed files, decisions, checks per card) |
| Objective | Run the full B-2 acceptance matrix against a fake provider with its own tokenizer/limit accounting, record the handoff, and update B-2.md. |

## Implementation

1. Consolidate/extend the scenario suite into `test/runtime/cline-sdk/context-budget-acceptance.test.ts` using `test/utilities/fake-openai-provider.ts` (configurable token ceiling, real HTTP, exact error shapes), covering every B-2.md acceptance scenario:
   - missing/mistaken provider metadata (fallback governs; a mistaken *larger* metadata value is capped by override/min-rule),
   - an explicit smaller server limit,
   - output reservations (reserve covers expected output; no double count),
   - tool-schema overhead (large schema JSON changes the trigger),
   - a large next tool result (bound + compaction before rejection),
   - near-limit history (0.79/0.81 matrix, no provider overflow error).
2. Measure and record (numbers, not prose): tested effective budget, before/after assembled request sizes (pre-B-2 vs post-B-2 on the B-1 repro fixture), trigger utilization at compaction, summarizer request size.
3. Dependency/SDK accounting: which behavior is SDK-owned vs Kanban-owned; whether any SDK release was required (if so, the pinned version in lockfiles; never node_modules patches).
4. Update `docs/plans/B-2.md`:
   - check off B-2.1–B-2.8 with links to the card handoffs,
   - fill in the **Handoff** section (tested effective budget, before/after request sizes, SDK responsibility, dependency release needed for deployment),
   - add the **patch ledger** rows (B ID, fork PR, upstream issue/PR, dependency revision, deployment SHA, retire-ability) for anything filed upstream or pinned.
5. Full gate: `npm test`, typecheck, production build (package.json scripts).

## Verification

- The acceptance suite passes on the fake provider with zero context-overflow errors.
- B-2.md is internally consistent (checklist, handoff, ledger all updated in one commit).

## Acceptance

- All six acceptance scenarios green; estimates labeled as estimates throughout.
- B-2.md states what was tested, with which SDK revision, and what deployment needs (dependency release or "none").
- No acceptance scenario is silently waived; any parked item is named with its blocker card and owner (B-3/B-4/etc. only where B-2.md assigns it there).

## Deployment

Per the handoff record — typically: deploy the fork SHA containing B-2; no new credentials; settings from B-2-9 are optional.

## Handoff

The updated B-2.md sections themselves are the handoff artifact; plus this card's measurement table.

## Stop conditions

- Any scenario exposes an SDK defect requiring an unreleased SDK change → open/attach the upstream issue per B-2.md policy (B-2 is the "SDK/configuration fix where appropriate" bug-fix track; no feature-discussion gate applies), pin the release when available, and mark that scenario blocked-with-evidence rather than waiving it.
- Measurements contradict an earlier card's recorded default → fix the card's code, re-run, and update both the card handoff and B-2.md; do not paper over with test-only changes.
