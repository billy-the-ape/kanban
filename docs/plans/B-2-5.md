# B-2-5 — Proactive trigger on the assembled request

Execution card for **B-2.4** of [B-2.md](./B-2.md). Compaction config plumbing is B-2-4. Read B-2.md first (B-2.4: "Trigger before the assembled request would exceed its input budget. Start evaluation around 80% utilization as a proposed tuning value, not a hardcoded universal answer; use measured behavior to choose defaults").

| Field | Value |
| --- | --- |
| Identity | B-2 / B-2.4 / card B-2-5 — trigger coverage + measured defaults |
| Starting point | fork `billy-the-ape/kanban`, base SHA `7f272590f185c284e49c9fe2daa2f9a44aae88cf` (record actual HEAD; SDK 0.0.38) |
| Prerequisites | B-2-3 (estimator), B-2-4 (config wired) |
| Objective | The compaction trigger evaluates the **assembled request** (system prompt + tool schemas + messages + expected output + safety margin) and fires before the provider limit is hit, with defaults chosen from measured behavior. |

## Investigation (record findings in the handoff)

- Read the installed SDK (`dist/extensions/context/basic-compaction.d.ts`, `compaction-shared.d.ts`: `getContextWindowTokens`, `triggerTokens`, `utilizationRatio` in `CoreCompactionContext`) and determine exactly what the trigger counts: messages only, or system + tools + messages?
- Decide the mechanism:
  - If the SDK trigger already covers the assembled request and honors `thresholdRatio` → this card is verification + default tuning (still a deliverable).
  - If it under-counts (e.g. ignores tool schemas/system) → wire a Kanban-side `compact` callback via `localRuntime.compaction` (`cline-session-runtime.ts` start, `localRuntime` block ~line 230) that uses B-2-3 `computeContextBudget` to decide compaction (return a compacted result or `undefined`); and/or file the SDK fix per B-2.md upstream policy.

## Implementation

1. `src/cline-sdk/cline-compaction-config.ts` — confirm `thresholdRatio` default 0.8 is in effect; expose it for B-2-9 settings.
2. If needed, `src/cline-sdk/cline-session-runtime.ts` — pass `localRuntime.compaction.compact` implementing the assembled-request check with the B-2-3 estimator (estimates labeled as estimates in logs).
3. Measurement runs against `test/utilities/fake-openai-provider.ts` (its own tokenizer/limit accounting) using the B-1 repro fixtures:
   - near-limit history at 0.79 / 0.81 utilization of the input budget,
   - a single large next tool result that must trigger compaction before the provider rejects (B-2.5 invariant: a large result must not bypass the budget merely because previous usage was low).
4. Choose defaults (`thresholdRatio`, `preserveRecentTokens`, `reserveTokens`) from the measurements; document the rationale with numbers.

## Verification

- New `test/runtime/cline-sdk/proactive-compaction.test.ts`:
  - near-limit history compacts **before** any provider context-overflow error,
  - large next tool result triggers compaction with headroom for expected output,
  - trigger fires at or below the configured ratio (assert via logged utilization or message-count change),
  - no provider rejection occurs in any scenario.
- `npm test`, typecheck.

## Acceptance

- Zero provider context-overflow errors across the near-limit matrix on the fake provider.
- Defaults are justified by recorded measurements, not copied from the SDK defaults, and estimates are labeled as estimates in logs/tests.

## Deployment

None.

## Handoff

Changed files, SDK trigger coverage findings (counts what?), measured utilization table, final defaults + rationale, any SDK fix filed (patch ledger entry).

## Stop conditions

- SDK trigger defect requires an unreleased SDK change → file the upstream bug (B-2 is the "SDK/configuration fix where appropriate" track), implement the Kanban-side `compact` callback as the interim if it keeps all scenarios green, park the rest with evidence.
- Measurements show 0.8 is unsafe for the served model (trigger too late) → adjust default; do not ship an unmeasured number.
